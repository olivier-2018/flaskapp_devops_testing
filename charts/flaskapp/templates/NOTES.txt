Thank you for installing flaskapp.

STATUS:
#######
- Helm chart {{ .Release.Name }} has been installed.
- Docker image: {{ .Values.image.name }}:{{ .Values.image.tag }}
- The application is running in your Kubernetes cluster.

ACCESS:
#######
- Option A: minikube service flaskapp -n helm-tutorial

- Option B: http://<NODE-IP>:30080
   where <NODE-IP> is the IP address of your Kubernetes node. You can find it using:
      kubectl get nodes -o wide  
      or
      minikube ip
